import './style.css';

export const parameters = {
    actions: { argTypesRegex: "^on[A-Z].*" },
    darkMode: {
        stylePreview: true,
        darkClass: 'is-dark-mode',
        lightClass: 'is-light-mode'
    }
};
